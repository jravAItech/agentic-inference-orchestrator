langgraph>=0.2.0
sentence-transformers>=2.2.0
numpy>=1.24.0
# Phase 2 (GPU, install in Colab/Kaggle only):
# tensorrt_llm
# tritonclient[all]
# Cloud (install only when deploying to Azure):
# azure-storage-blob
# azure-search-documents
# azure-core
# fastapi
# uvicorn
